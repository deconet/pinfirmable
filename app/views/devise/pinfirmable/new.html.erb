<div class="pinfirmable-panel">
  <h1><%= t("confirm.title") %></h1>
  <p><%= simple_format t("confirm.body") %></p>
  <%= image_tag "loader.gif", id: "pinfirmable-loading-gif", style: "display: none;" %>
  <%= form_tag("/users/pinfirmable", method: "post", id: "pinfirmable-form") do %>
    <div id="pinfirmable-pin-inputs">
      <%= text_field_tag "digits[]", "", id: "digits_1", type: "tel", class: "pin-input", maxlength: "1", onKeyPress: "return pinfirmable.autoTab(event)", onKeyDown: "return pinfirmable.handleBackspacePress(event)", onFocus: "return this.select()", autocomplete: "off", onPaste: "return pinfirmable.pastePin(event)", autofocus: true %>
      <%= text_field_tag "digits[]", "", id: "digits_2", type: "tel", class: "pin-input", maxlength: "1", onKeyPress: "return pinfirmable.autoTab(event)", onKeyDown: "return pinfirmable.handleBackspacePress(event)", onFocus: "return this.select()", autocomplete: "off", onPaste: "return pinfirmable.pastePin(event)" %>
      <%= text_field_tag "digits[]", "", id: "digits_3", type: "tel", class: "pin-input", maxlength: "1", onKeyPress: "return pinfirmable.autoTab(event)", onKeyDown: "return pinfirmable.handleBackspacePress(event)", onFocus: "return this.select()", autocomplete: "off", onPaste: "return pinfirmable.pastePin(event)" %>
      <span class="pin-separator">&nbsp;</span>
        <%= text_field_tag "digits[]", "", id: "digits_4", type: "tel", class: "pin-input", maxlength: "1", onKeyPress: "return pinfirmable.autoTab(event)", onKeyDown: "return pinfirmable.handleBackspacePress(event)", onFocus: "return this.select()", autocomplete: "off", onPaste: "return pinfirmable.pastePin(event)" %>
        <%= text_field_tag "digits[]", "", id: "digits_5", type: "tel", class: "pin-input", maxlength: "1", onKeyPress: "return pinfirmable.autoTab(event)", onKeyDown: "return pinfirmable.handleBackspacePress(event)", onFocus: "return this.select()", autocomplete: "off", onPaste: "return pinfirmable.pastePin(event)" %>
        <%= text_field_tag "digits[]", "", id: "digits_6", type: "tel", class: "pin-input", maxlength: "1", onKeyPress: "return pinfirmable.autoTab(event)", onKeyDown: "return pinfirmable.handleBackspacePress(event)", onFocus: "return this.select()", autocomplete: "off", onPaste: "return pinfirmable.pastePin(event)" %>
    </div>
    <%= submit_tag("GO!", id: "pinfirmable-noscript") %>
  <% end %>
  <% if @locked_out %>
    <div class="pinfirmable-alert">
      <%= t("confirm.rate_limit") %>
    </div>
  <% end %>

  <%= form_tag("/users/pinfirmable/resend_email", method: "post", id: "pinfirmable-resend-form") do %>
    <%= submit_tag(t("confirm.no_email")) %>
  <% end %>
</div>
